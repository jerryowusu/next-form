/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: '/jerry-in-react',
    output: 'export', // enables static exports
    reactStrictMode: true,
};

export default nextConfig;
